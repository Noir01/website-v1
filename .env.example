# PartyServer Configuration
# For local development (Wrangler dev server runs on port 8787)
PUBLIC_PARTYKIT_HOST=localhost:8787

# For production, set this to your deployed Cloudflare Worker URL (without https://)
# Example: PUBLIC_PARTYKIT_HOST=portfolio-multiplayer-cursor.YOUR_SUBDOMAIN.workers.dev
